branch_protection:
  policies:
    - target: branch
      matches: "main"
      rules:
        - prevent_deletion
        - prevent_force_push
        - prevent_non_pr_changes

pull_request: 
  branch_cleanup: 
    matches: ["**"]
